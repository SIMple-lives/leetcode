#include <stdio.h>

int** permute(int* nums, int numsSize, int* returnSize, int** returnColumnSizes) 
{
    
}

int main()
{
    int numsSize =0;
    scanf("%d",&numsSize);
    int returnSize =0;
    int nums[numsSize];
    for(int i=0;i<numsSize;i++)
    {
        scanf("%d",&returnSize);
    }
    
    return 0;
}